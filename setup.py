from setuptools import setup

setup(
   name='math_quiz',
   version='1.0',
   description='A Math Quiz Game',
   author='Anosh',
   author_email='ahmadanosh.ar@gmail.com',
   packages=['math_quiz'],  #same as name
   install_requires=[], #external packages as dependencies
)